# 游戏设计与开发

## Windows 平台

### 注意项

- 在Windows坐标系统中，Y轴从上到下递增，所以 bottom 值大于 top 值
- Windows中使用字符串要注意

创建窗口

消息循环

分配缓冲区

位图绘制像素（rgb）


### 数学

取模运算将绝对索引转换为相对索引


## 游戏架构

一帧的延迟，优先渲染，在这一帧渲染完和下一帧开始渲染前的空隙中更新游戏状态，这样是为了保持良好的性能，避免接受输入更新状态再渲染。
